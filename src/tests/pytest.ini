[pytest]
addopts = --tb=short
filterwarnings =
    ignore::DeprecationWarning
markers = 
    container: mark test run only with containers up.
    slow: mark test run more slowly becouse timeouts or errors.
    broker_only: Test with rabbitmq broker on.
